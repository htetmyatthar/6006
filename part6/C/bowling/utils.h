#include <stdio.h>
#include <stdlib.h>
#include <math.h>

// bowl_recursive returns the maximum points that can obtained
// by playing in a set of rules that is described in README.
// NOTE: USES THE RECURSIVE APPROACH(top down)
int bowl_recursive(int values[], int length);

// bowl_iterative returns the maximum points that can obtained
// by playing in a set of rules that is described in README.
// NOTE: USES THE ITERATIVE APPROACH(bottom up)
int bowl_iterative(int values[], int length);
