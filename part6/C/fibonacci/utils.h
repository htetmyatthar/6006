#include <stdio.h>
#include <assert.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

// fibonacci_plain returns the nth number in the fibonacci sequence,
// using naive implementation.
int fibonacci_plain(int n);

// fibonacci_recursive returns the nth number in the fibonacci sequence,
// using recursion and memo approach.
int fibonacci_recursive(int n);

// fibonacci_iterative returns the nth number in the fibonacci sequence,
// using iterative approach.
int fibonacci_iterative(int n);
